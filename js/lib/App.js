define(
    ['backbone'],
    function (Backbone) {
        var init = function() {
            console.log('Init application');
        }

        return {
            init : init
        }
    }
);
